version: '3.8'

services:
  meilisearch:
    image: getmeili/meilisearch:v1.12
    ports:
      - "7700:7700"
    environment:
      - MEILI_ENV=development
      - MEILI_MASTER_KEY=quran_search_master_key
    volumes:
      - ./meili_data:/meili_data
    restart: unless-stopped
